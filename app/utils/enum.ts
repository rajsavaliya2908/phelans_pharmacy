export enum SignIn {
  UserName,
  Password,
}

export enum Register {
  SelectLocal,
  Email,
  Password,
  Name,
  TelePhone,
  Address,
  BirthDate,
}

export enum AddUser {
  SelectLocal,
  Email,
  Password,
  Name,
  TelePhone,
  Address,
  BirthDate,
}

export enum HomeMenu {
  Prescription,
  BookService,
  MedicineReminder,
  MyPharmacy,
  Locator,
  User,
}

export enum PrescriptionMenu {
  Order,
  Reminder,
}

export enum ReorderReminderTime {
  Time,
  Date,
}

export enum MedicineReminder {
  Time,
  Date,
}
